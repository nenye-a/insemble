/* eslint no-useless-call: "off" */
// Most tests taken from DELETED_URL_WITH_CREDENTIALS"0", {
		set: function (x) { throw new Error("Setter called: " + x); }
	});
	a.deep(t.call(MyType, "abc"), { 0: "abc", length: 1 }, "Define, not set");
};
