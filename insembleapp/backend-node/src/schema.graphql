# Root Schema

# import Feedback from "./generated/prisma.graphql"

input CategoryWhereInput {
    category: String
    category_contains: String
}

# =========
# RESOLVERS
# =========

type Query {
    """
    feedback
    """
    categories(where: CategoryWhereInput): [String!]! # TODO:

    feedback: Feedback!
    feedbacks(
      where: FeedbackWhereInput
    ): [Feedback!]!
}
