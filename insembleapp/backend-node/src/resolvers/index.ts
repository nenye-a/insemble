import { categories } from './category/categoryQuery';
import { feedback, feedbacks } from './feedback/feedbackQuery';

export default {
    Query: {
        categories,

        feedback,
        feedbacks,
    },
};
